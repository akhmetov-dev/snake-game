jar {
    manifest {
        attributes "Main-Class": "ru.bmstu.iu4.Starter"
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

gradlew.bat jar // Создание джарника
java -jar Snake-1.0.jar // Запуск игры